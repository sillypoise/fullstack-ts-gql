{
    "$schema": "http://json.schemastore.org/nodemon",
    "verbose": true,
    "execMap": {
        "js": "node"
    },
    "watch": ["dist", "build.*.mjs", "modules/**/typedefs/*.graphql"],
    "ext": "ts,js,json,html",
    "env": {
        "NODE_ENV": "development"
    }
}

{
    "$schema": "http://json.schemastore.org/nodemon",
    "verbose": true,
    "ignore": ["src"],
    "execMap": {
        "js": "node"
    },
    "watch": ["dist", "build.*.mjs", "*.graphql"],
    "ext": "ts,js,json,html",
    "env": {
        "NODE_ENV": "development"
    }
}
